# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

## Expo Go / App Store

This project targets **Expo SDK 54** so it works with the Expo Go build currently on the Apple App Store / Google Play (client build ~1017756, app version 54.x). Newer SDKs (55–57) are not yet available in store Expo Go while Apple review is pending — “latest from the store” can still mean SDK 54.
